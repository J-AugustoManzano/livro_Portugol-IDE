// programa LACO2
inicio
  variavel inteiro N, I, R 
  escrever "Entre um valor numerico: "
  ler N
  I <- 1
  enquanto nao (I > 10) faz
    R <- N * I
    escrever N, " X ", I, " = ", R, "\n"
    I <- I + 1
  fimenquanto
fim
